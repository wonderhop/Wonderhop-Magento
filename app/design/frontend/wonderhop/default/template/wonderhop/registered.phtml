<script type="text/javascript">
    
    <?php foreach($this->trackEventsQueue() as $event => $data) { 
        $data = empty($data) ? NULL : Mage::helper('core')->jsonEncode($data); ?>
        mixpanel.track("<?php echo $event; ?>"<?php echo $data ? (','.$data) : ''; ?> );
    <?php } ?>
    
</script>
